const companyName="SLA";
companyName="EMC";
console.log(companyName);

it should be error.
because the constant variable cannot reassigned to a diff value.
it is the rule for constant in js.
but the other variables var & let can change it's value.
